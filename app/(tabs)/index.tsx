import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import React from "react";

const Chats: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Chats is working</Text>
    </SafeAreaView>
  );
};

export default Chats;
